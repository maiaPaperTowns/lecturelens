# Base URL the SPA uses to reach the API.
# Dev (vite proxy): leave as /api    |    Direct: http://localhost:8000/api
VITE_API_BASE_URL=/api

# Optional: where `vite dev` proxies /api during local development
VITE_PROXY_TARGET=http://localhost:8000
